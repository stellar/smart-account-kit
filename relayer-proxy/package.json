{
  "name": "smart-account-relayer-proxy",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:production": "wrangler deploy --env production",
    "test": "vitest run"
  },
  "dependencies": {
    "@openzeppelin/relayer-plugin-channels": "^0.20.0",
    "@stellar/stellar-sdk": "^16.3.0",
    "hono": "^4.13.7"
  },
  "devDependencies": {
    "typescript": "^6.0.2",
    "wrangler": "^4.130.0"
  }
}
